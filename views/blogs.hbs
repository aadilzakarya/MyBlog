<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Articles</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1>Articles</h1>
        <p>Welcome, {{username}}!</p>

        <div class="mb-3">
            <a href="/my-profile" class="btn btn-primary">My Profile</a>
            <button type="button" class="btn btn-primary" onclick="location.href='/add-blog';">Add Blog</button>
            <a href="/logout" class="btn btn-primary">Logout</a>
            <a href="/contact-us" class = "btn btn-primary">Contact Us</a>
        </div>

        <form action="/dashboard/search" method="GET" class="form-inline mb-3">
            <input type="text" class="form-control mr-sm-2" name="searchTerm" placeholder="Search articles">
            <button type="submit" class="btn btn-outline-success my-2 my-sm-0">Search</button>
        </form>

        <!-- Place to show search results -->
        {{#if searchResults}}
            <div>
                {{#each searchResults}}
                    <div class="card mb-4">
                        <div class="card-body">
                            <h3 class="card-title">{{this.title}}</h3>
                            <p class="card-text">{{this.content}}</p>
                            <p class="card-text"><small class="text-muted">Author: {{this.author}}</small></p>
                            <p class="card-text"><small class="text-muted">Posted on: {{this.createdAt}}</small></p>
                        </div>
                    </div>
                {{/each}}
            </div>
        {{else}}
           
        {{/if}}

        {{#each articles}}
            <div class="card mb-4">
                <div class="card-body">
                    <h2 class="card-title">{{this.title}}</h2>
                    <p class="card-text"><small class="text-muted">Estimated Reading Time: {{lookup ../readingTimes @index}}</small></p>
                    <p class="card-text">{{this.content}}</p>
                    <p class="card-text"><small class="text-muted">Written by: {{this.author}}</small></p>
                    <p class="card-text"><small class="text-muted">Posted on: {{this.createdAt}}</small></p>
                </div>
            </div>
        {{else}}
            <p class="alert alert-info">No articles found.</p>
        {{/each}}
    </div>

    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
